- hosts: localhost
  tasks:
    - name: Copiar o arquivo
      copy:
        src: /var/jenkins_home/workspace/teste/docker-dotnet-core
        dest: /var/ansible/
        owner: ansible
        group: ansible
        mode: '0644'
